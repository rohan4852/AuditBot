# Copy this file to `.env` and fill values (do NOT commit your real API key).
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# Optional: path to tessdata folder (often ends with "\\tessdata" on Windows)
TESSDATA_PREFIX=C:\\Program Files\\Tesseract-OCR\\tessdata

# Optional: full path to tesseract executable if not on PATH
TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe

# Optional: Poppler bin path if pdf2image cannot find it on PATH
POPPLER_PATH=C:\\path\\to\\poppler\\Library\\bin
